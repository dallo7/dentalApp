#ui-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent black background */
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 999; /* Ensure the loading container is displayed on top of other elements */
}

#content {
    transition: opacity 1s ease-in-out; /* Smooth transition for content */
}

#ui-container.hidden {
    opacity: 0; /* Hide the loading container after content loads */
    transition: opacity 0.5s ease-in-out; /* Smooth transition for hiding */
}


#ui-container1 {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent black background */
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 999; /* Ensure the loading container is displayed on top of other elements */
}

#content1 {
    transition: opacity 1s ease-in-out; /* Smooth transition for content */
}

#ui-container1.hidden {
    opacity: 0; /* Hide the loading container after content loads */
    transition: opacity 0.5s ease-in-out; /* Smooth transition for hiding */
}


#ui-container2 {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent black background */
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 999; /* Ensure the loading container is displayed on top of other elements */
}

#content2 {
    transition: opacity 1s ease-in-out; /* Smooth transition for content */
}

#ui-container2.hidden {
    opacity: 0; /* Hide the loading container after content loads */
    transition: opacity 0.5s ease-in-out; /* Smooth transition for hiding */
}


#ui-container3 {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent black background */
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 999; /* Ensure the loading container is displayed on top of other elements */
}

#content3 {
    transition: opacity 1s ease-in-out; /* Smooth transition for content */
}

#ui-container3.hidden {
    opacity: 0; /* Hide the loading container after content loads */
    transition: opacity 0.5s ease-in-out; /* Smooth transition for hiding */
}

@keyframes fadeIn {
    0% {opacity: 0;}
    100% {opacity: 1;}
}

#ui-container4 {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent black background */
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 999; /* Ensure the loading container is displayed on top of other elements */
}

#content4 {
    transition: opacity 1s ease-in-out; /* Smooth transition for content */
}

#ui-container4.hidden {
    opacity: 0; /* Hide the loading container after content loads */
    transition: opacity 0.5s ease-in-out; /* Smooth transition for hiding */
}
